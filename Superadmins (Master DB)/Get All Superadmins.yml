info:
  name: Get All Superadmins
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/superadmin"
  headers:
    - name: Authorization
      value: Bearer {{superadmin_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista todos los superadmins.
