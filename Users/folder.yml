info:
  name: Users
  type: folder
  seq: 2

request:
  auth: inherit

docs:
  content: "Gestión de usuarios (CRUD). Solo ADMIN. Todos los endpoints requieren `Authorization` + `X-Tenant-ID`."
  type: text/markdown
