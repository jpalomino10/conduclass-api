info:
  name: Get All Organizations
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/organization"
  headers:
    - name: Authorization
      value: Bearer {{superadmin_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista todas las organizaciones.
